# Video Drawing — Workspace Setup

Quick start:

```powershell
npm install
npm run dev
npm test
```

Notes:
- Project uses Vite + React and Vitest for TDD.
- Run `npm test` to execute tests once; use `npm run test:watch` for TDD loop.
